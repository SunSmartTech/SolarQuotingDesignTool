/*
------------------------------------------------------------------------------ 
This code was generated by Amplication. 
 
Changes to this file will be lost if the code is regenerated. 

There are other ways to to customize your code, see this doc to learn more
https://docs.amplication.com/how-to/custom-code

------------------------------------------------------------------------------
  */
import * as common from "@nestjs/common";
import * as swagger from "@nestjs/swagger";
import { isRecordNotFoundError } from "../../prisma.util";
import * as errors from "../../errors";
import { Request } from "express";
import { plainToClass } from "class-transformer";
import { ApiNestedQuery } from "../../decorators/api-nested-query.decorator";
import { GrpcMethod } from "@nestjs/microservices";
import { PublicFileService } from "../publicFile.service";
import { PublicFileCreateInput } from "./PublicFileCreateInput";
import { PublicFileWhereInput } from "./PublicFileWhereInput";
import { PublicFileWhereUniqueInput } from "./PublicFileWhereUniqueInput";
import { PublicFileFindManyArgs } from "./PublicFileFindManyArgs";
import { PublicFileUpdateInput } from "./PublicFileUpdateInput";
import { PublicFile } from "./PublicFile";

export class PublicFileGrpcControllerBase {
  constructor(protected readonly service: PublicFileService) {}
  @common.Post()
  @swagger.ApiCreatedResponse({ type: PublicFile })
  @GrpcMethod("PublicFileService", "createPublicFile")
  async createPublicFile(
    @common.Body() data: PublicFileCreateInput
  ): Promise<PublicFile> {
    return await this.service.createPublicFile({
      data: data,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.Get()
  @swagger.ApiOkResponse({ type: [PublicFile] })
  @ApiNestedQuery(PublicFileFindManyArgs)
  @GrpcMethod("PublicFileService", "publicFiles")
  async publicFiles(@common.Req() request: Request): Promise<PublicFile[]> {
    const args = plainToClass(PublicFileFindManyArgs, request.query);
    return this.service.publicFiles({
      ...args,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
  }

  @common.Get("/:id")
  @swagger.ApiOkResponse({ type: PublicFile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("PublicFileService", "publicFile")
  async publicFile(
    @common.Param() params: PublicFileWhereUniqueInput
  ): Promise<PublicFile | null> {
    const result = await this.service.publicFile({
      where: params,
      select: {
        createdAt: true,
        id: true,
        updatedAt: true,
      },
    });
    if (result === null) {
      throw new errors.NotFoundException(
        `No resource was found for ${JSON.stringify(params)}`
      );
    }
    return result;
  }

  @common.Patch("/:id")
  @swagger.ApiOkResponse({ type: PublicFile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("PublicFileService", "updatePublicFile")
  async updatePublicFile(
    @common.Param() params: PublicFileWhereUniqueInput,
    @common.Body() data: PublicFileUpdateInput
  ): Promise<PublicFile | null> {
    try {
      return await this.service.updatePublicFile({
        where: params,
        data: data,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }

  @common.Delete("/:id")
  @swagger.ApiOkResponse({ type: PublicFile })
  @swagger.ApiNotFoundResponse({ type: errors.NotFoundException })
  @GrpcMethod("PublicFileService", "deletePublicFile")
  async deletePublicFile(
    @common.Param() params: PublicFileWhereUniqueInput
  ): Promise<PublicFile | null> {
    try {
      return await this.service.deletePublicFile({
        where: params,
        select: {
          createdAt: true,
          id: true,
          updatedAt: true,
        },
      });
    } catch (error) {
      if (isRecordNotFoundError(error)) {
        throw new errors.NotFoundException(
          `No resource was found for ${JSON.stringify(params)}`
        );
      }
      throw error;
    }
  }
}
